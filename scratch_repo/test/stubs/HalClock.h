// Host stub for HalClock.h. The on-device header provides RTC helpers used by
// main.cpp / sleep code; RainmakerSchedule.cpp only includes it as a forward
// dependency and does not call any symbol. An empty header is sufficient for
// the host build to compile.
#pragma once
